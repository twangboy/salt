Fixed salt-api memory growth on busy masters by using a plain deque for the
ZeroMQ REQ message queue instead of Tornado’s Queue (which retained extra deque
and waiter state per short-lived request channel used by the REST API).
